import React from "react";

const ViewAllPatient = (props) => {
  // const { name, patientName, number, email } = props.data;
  return (
    <div>
      {/* <tr>
        <th>{patientName}</th>
        <th>{email}</th>
        <th>{number}</th>
        <th>
          <button>pendin</button>
        </th>
      </tr> */}
    </div>
  );
};

export default ViewAllPatient;
